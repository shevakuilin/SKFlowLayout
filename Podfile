platform :ios, ’10.0’
inhibit_all_warnings! # 关闭 CocoaPods 警告
install! 'cocoapods', :disable_input_output_paths => true # 解决改动pod后无法立即生效

use_frameworks!
target 'FlowLayout' do
  # Layout
  pod 'Masonry'
  # Media
  pod 'SDWebImage'
end
